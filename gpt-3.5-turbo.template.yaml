llm:
  provider: openai
  config:
    model: 'gpt-3.5-turbo'
    temperature: 0.5
    api_key: ${openai_api_key}
vectordb:
  provider: chroma
  config:
    dir: ${db_path}
embedder:
  provider: openai
  config:
    api_key: ${openai_api_key}
